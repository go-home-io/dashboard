// Intervals in ms
export const CONNECTION_TIMEOUT = 3000;
export const PING_INTERVAL = 5000;

// Max attempts to send messageInfo
export const MAX_ATTEMPTS = 5;

// Delay before next attempt to send message to server
export const NEXT_ATTEMPT_DELAY = 500;